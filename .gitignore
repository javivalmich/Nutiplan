# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Env files (CRITICAL - Supabase/Vercel secrets)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# OS files
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw*
lint_output.txt
*lint_output.txt

# Motor viejo deprecado — congelado, no se lee/edita/importa (ver CLAUDE.md). Archivar en Fase 7.
main_buildPlan_tmp.js

# Scripts de análisis/medición ad-hoc (no producción, no tests CI). Evidencia
# que se quiera conservar va versionada en docs/evidence/, no aquí.
analysis/

# Scratch y diagnóstico (no versionado)
scripts/diag/
# Salida generada del cuaderno editorial (no versionada). Excepción D-039:
# el lateral dishCompositionConfirmations.json se niega explícitamente --
# ignorar el directorio (con /) no re-ignora un archivo ya tracked, pero
# negarlo aquí cierra la trampa latente si alguna vez se destrackeara.
scripts/phaseB2/output/*
!scripts/phaseB2/output/dishCompositionConfirmations.json